import org.votegrep._
import java.net.URL
import scala.concurrent._
import scala.concurrent.duration._
import scala.concurrent.ExecutionContext
import ExecutionContext.Implicits.global
/*
val feedStr = "https://sjc.granicus.com/ViewPublisherRSS.php?view_id=3&mode=minutes"
val url = new URL(feedStr)
val posts = Await.result(SyndPosts.fromURL(url), 10 seconds)

*/
